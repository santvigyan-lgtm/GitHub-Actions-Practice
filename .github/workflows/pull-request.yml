name: Pull Request

on: 
    pull_request:
        branches: 
            - "main"

jobs:
    linting_code:
        runs-on: ubuntu-latest

        steps:
            - name: linting code
              run: echo "linting code"
              